# TODO - Ajuste de Títulos por Nível (mais fáceis)

- [ ] Atualizar regra de título por nível (0-99) com as novas categorias fornecidas.
- [ ] Atualizar `frontend/src/components/Ranking.jsx` para usar esses títulos por nível.
- [ ] (Opcional) Ajustar também qualquer outro lugar que mostre título (buscar no frontend/backend por `getTitleByLevel`).
- [ ] Rodar `npm test`/`npm run build`/`npm run dev` para validar.

